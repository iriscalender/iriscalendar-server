package com.javaproject.iriscalendar.controller;

import org.springframework.web.bind.annotation.*;

import javax.validation.Valid;

@RestController
@RequestMapping("calendar")
public class CalendarController {
//    public ? addAutomaticCalendar(@RestController("Authorization") String auth, @RequestBody @Valid AutomaticCalendarParam automaticCalendarParam) {
//
//    }
//    public ? calendar(@RequestHeader("Authorization") String auth, @PathVariable Integer id) {
//
//    }

}
